// This is a bootstrapping script that you run with Node in order to set everything up.

/*
- check for docker
- check for caddy

  with docker:
    cd src/docker
    run docker build -t local-base-image .

  with caddy:
    ...somehow make sure the root CA is installed?
    set up a Caddyfile with the editor.com.localhost binding

*/

